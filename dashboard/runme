server=$(which parse-dashboard)

[ -z $server ] && npm install -g parse-dashboard
[ -f ./settings ] && source ./settings
parse-dashboard --port $DASHPORT --appId $APPID --masterKey $KEY --serverURL $SERVER_URL --appName $appName

